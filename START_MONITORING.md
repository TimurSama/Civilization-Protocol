# 🚀 Запуск автоматического мониторинга деплоя

## Быстрый старт

### 1. Установите токен (если еще не установлен):

```powershell
$env:GITHUB_TOKEN = "YOUR_TOKEN_HERE"
```

### 2. Запустите фоновый мониторинг:

```powershell
.\github\scripts\watch-deploy.ps1
```

### 3. Скрипт будет:

- ✅ Проверять статус деплоя каждые 30 секунд
- ✅ Автоматически исправлять ESLint ошибки
- ✅ Коммитить и пушить исправления
- ✅ Показывать статус в реальном времени

## Параметры

```powershell
# Изменить интервал проверки (по умолчанию 30 секунд)
.\github\scripts\watch-deploy.ps1 -IntervalSeconds 60

# Отключить автоисправление
.\github\scripts\watch-deploy.ps1 -AutoFix:$false
```

## Что делает скрипт:

1. **Мониторинг** - проверяет статус последнего workflow каждые 30 секунд
2. **Обнаружение ошибок** - если деплой провалился, анализирует причины
3. **Автоисправление**:
   - Запускает `npm run lint -- --fix`
   - Проверяет TypeScript ошибки
   - Коммитит и пушит исправления
4. **Уведомления** - показывает статус в консоли

## Остановка

Нажмите `Ctrl+C` для остановки мониторинга.

## GitHub Actions автоисправление

Также настроен GitHub Actions workflow (`.github/workflows/auto-fix.yml`), который:
- Автоматически запускается при провале деплоя
- Исправляет ошибки линтера
- Коммитит исправления автоматически

Это работает даже когда скрипт не запущен!

