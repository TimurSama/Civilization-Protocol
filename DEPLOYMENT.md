# Деплой Civilization Protocol

## 🚀 Автоматический деплой через Vercel + GitHub Actions

### Шаг 1: Настройка Vercel

1. **Зарегистрируйтесь на [Vercel](https://vercel.com)**
   - Используйте ваш GitHub аккаунт для входа
   - Подтвердите email если потребуется

2. **Импортируйте проект**
   - Нажмите "Add New..." → "Project"
   - Выберите репозиторий `TimurSama/Civilization-Protocol`
   - Vercel автоматически определит Next.js проект

3. **Получите необходимые данные**
   - После импорта проекта зайдите в Settings → General
   - Скопируйте **Project ID**
   - Зайдите в Account Settings → General
   - Скопируйте **Organization ID**

### Шаг 2: Настройка GitHub Secrets

1. **Получите Vercel Token**
   - Зайдите в Vercel Account Settings → Tokens
   - Создайте новый токен: "Create Token"
   - Назовите его `GitHub Actions`
   - Сохраните токен (он больше не будет показан)

2. **Добавьте секреты в GitHub**
   - В репозитории перейдите в Settings → Secrets and variables → Actions
   - Добавьте 3 секрета:
     ```
     VERCEL_TOKEN = ваш_токен_vercel
     ORG_ID = ваш_organization_id
     PROJECT_ID = ваш_project_id
     ```

### Шаг 3: Автоматический деплой

После настройки секретов:
- **Каждый push в master/main** → автоматический деплой на production
- **Каждый Pull Request** → автоматический деплой на preview URL
- GitHub Actions будет собирать и деплоить проект автоматически

## 🛠️ Альтернативные способы деплоя

### 1. Vercel CLI (локальный деплой)
```bash
npm i -g vercel
vercel --prod
```

### 2. Netlify
```bash
npm run build
# Загрузить папку .next в Netlify
```

### 3. Docker (для собственного сервера)
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 📝 Проверка деплоя

После успешного деплоя:
- Production URL: `https://civilization-protocol.vercel.app`
- Preview URLs: доступны в PR и в Vercel dashboard
- Статус сборки: в GitHub Actions tab

## 🔧 Возможные проблемы

1. **Build ошибки** - проверьте `npm run build` локально
2. **Environment variables** - добавьте в Vercel Settings
3. **Permissions** - убедитесь что GitHub Actions включен в репозитории

## 📊 Мониторинг

- Vercel Analytics для мониторинга производительности
- GitHub Actions для логов сборки
- Vercel Logs для отладки production ошибок
