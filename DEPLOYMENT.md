# Деплой VODeco Platform

## 🚀 Автоматический деплой через Vercel + GitHub

### ✅ Конфигурация уже настроена!

Проект готов к автоматическому деплою. Файлы конфигурации:
- ✅ `vercel.json` - настройки Vercel
- ✅ `.github/workflows/deploy.yml` - GitHub Actions workflow
- ✅ `next.config.ts` - оптимизация для Vercel
- ✅ `.vercelignore` - исключения для деплоя

### Шаг 1: Подключение к Vercel (если ещё не подключено)

1. **Зарегистрируйтесь на [Vercel](https://vercel.com)**
   - Используйте ваш GitHub аккаунт для входа

2. **Импортируйте проект**
   - Нажмите "Add New..." → "Project"
   - Выберите ваш репозиторий VODeco
   - Vercel автоматически определит Next.js проект
   - Нажмите "Deploy"

3. **Vercel автоматически:**
   - Определит Next.js 16
   - Установит зависимости (`npm install`)
   - Соберёт проект (`npm run build`)
   - Задеплоит на production

### Шаг 2: Настройка GitHub Secrets (для GitHub Actions)

Если хотите использовать GitHub Actions для дополнительного контроля:

1. **Получите Vercel Token**
   - Зайдите в Vercel Account Settings → Tokens
   - Создайте новый токен: "Create Token"
   - Назовите его `GitHub Actions`
   - Сохраните токен

2. **Получите ID проекта и организации**
   - В Vercel проекте: Settings → General → Project ID
   - Account Settings → General → Organization ID

3. **Добавьте секреты в GitHub**
   - В репозитории: Settings → Secrets and variables → Actions
   - Добавьте секреты:
     ```
     VERCEL_TOKEN = ваш_токен_vercel
     ORG_ID = ваш_organization_id
     PROJECT_ID = ваш_project_id
     ```

### Шаг 3: Автоматический деплой

**Vercel автоматически деплоит:**
- ✅ **Каждый push в main/master** → production деплой
- ✅ **Каждый Pull Request** → preview деплой (уникальный URL)
- ✅ **Автоматическая сборка** при каждом коммите

**GitHub Actions (опционально):**
- Проверяет код линтером
- Собирает проект
- Деплоит на Vercel (если настроены секреты)

## 🛠️ Альтернативные способы деплоя

### 1. Vercel CLI (локальный деплой)
```bash
npm i -g vercel
vercel --prod
```

### 2. Netlify
```bash
npm run build
# Загрузить папку .next в Netlify
```

### 3. Docker (для собственного сервера)
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

## 📝 Проверка деплоя

После успешного деплоя:
- **Production URL**: `https://vodeco.vercel.app` (или ваш кастомный домен)
- **Preview URLs**: доступны в каждом PR и в Vercel dashboard
- **Статус сборки**: 
  - В Vercel Dashboard → Deployments
  - В GitHub → Actions tab (если настроены секреты)

## 🔧 Environment Variables

Добавьте в Vercel Settings → Environment Variables:

**Обязательные:**
```
DATABASE_URL=your_database_url
JWT_SECRET=your_jwt_secret
NEXT_PUBLIC_API_URL=https://your-api-url.com
```

**Опциональные:**
```
GOOGLE_API_KEY=your_google_api_key
TELEGRAM_BOT_TOKEN=your_telegram_token
EMAIL_SERVICE_API_KEY=your_email_key
```

## 📊 Мониторинг

- **Vercel Analytics**: автоматически включён для мониторинга производительности
- **Vercel Logs**: для отладки production ошибок
- **GitHub Actions**: логи сборки (если настроены секреты)

## 🔧 Возможные проблемы

1. **Build ошибки** - проверьте `npm run build` локально
2. **Environment variables** - добавьте в Vercel Settings
3. **Permissions** - убедитесь что GitHub Actions включен в репозитории

## 📊 Мониторинг

- Vercel Analytics для мониторинга производительности
- GitHub Actions для логов сборки
- Vercel Logs для отладки production ошибок
