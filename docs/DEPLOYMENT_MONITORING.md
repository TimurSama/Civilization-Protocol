# Мониторинг и автоматизация деплоя

## 🔍 Как отслеживать деплой

### Варианты мониторинга:

1. **GitHub Actions Dashboard**
   - URL: https://github.com/TimurSama/Civilization-Protocol/actions
   - Видно статус каждого workflow
   - Логи в реальном времени
   - Уведомления при ошибках

2. **Vercel Dashboard**
   - URL: https://vercel.com/dashboard
   - Видно статус каждого деплоя
   - Автоматические уведомления на email

3. **GitHub Status Checks**
   - Статус отображается в каждом PR
   - ✅ Green = успешно
   - ❌ Red = ошибка

## 🤖 Автоматизация

### Что уже настроено:

✅ **GitHub Actions Workflow** (`deploy.yml`)
- Автоматическая сборка при push
- Проверка линтера
- TypeScript проверка через build
- Автоматический деплой на Vercel (если настроены секреты)

✅ **Build Check Workflow** (`build-check.yml`)
- Проверка типов TypeScript
- Проверка Prisma схемы
- Запускается на Pull Requests

## 📧 Уведомления

### Настройка email уведомлений:

1. **GitHub**
   - Settings → Notifications → Actions
   - Включите "Workflow runs"

2. **Vercel**
   - Settings → Notifications
   - Включите email уведомления

## 🔧 Ручной запуск проверок

```bash
# Локальная проверка типов
npm run type-check

# Локальная сборка
npm run build

# Проверка Prisma схемы
npm run db:validate
```

## ❌ Что делать при ошибках сборки

1. **Проверьте логи GitHub Actions**
   - Откройте failed workflow
   - Посмотрите логи шага "Build project"

2. **Проверьте типы локально**
   ```bash
   npm run type-check
   ```

3. **Проверьте сборку локально**
   ```bash
   npm run build
   ```

4. **Исправьте ошибки и закоммитьте**
   ```bash
   git add .
   git commit -m "fix: описание исправления"
   git push origin master
   ```

## 🚀 Будущие улучшения

Планируется добавить:
- Автоматическое исправление известных ошибок (prettier, eslint --fix)
- Автоматические PR с исправлениями
- Интеграцию с ботом для уведомлений в Telegram/Discord
- Dashboard для мониторинга статуса деплоя

## 💡 Ограничения

К сожалению, AI ассистент не может:
- ⛔ Подключаться напрямую к GitHub API
- ⛔ Мониторить деплои в реальном времени
- ⛔ Автоматически создавать коммиты с исправлениями без вашего участия

Но может:
- ✅ Помочь исправить ошибки, которые вы скопируете
- ✅ Настроить workflows для лучшей обработки ошибок
- ✅ Предложить улучшения CI/CD

