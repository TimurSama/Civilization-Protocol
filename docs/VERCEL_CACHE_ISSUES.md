# Решение проблем с кешем на Vercel

## Проблема: Сайт показывает старую версию после деплоя

### 🔍 Возможные причины:

1. **Кеш Vercel** - Vercel может использовать кеш старой сборки
2. **Браузерный кеш** - браузер кеширует статические файлы
3. **Production branch** - Vercel деплоит из другой ветки
4. **Preview deployment** - деплой был в preview, а не в production

---

## ✅ Решения:

### 1. Форсировать пересборку в Vercel

**Вариант A: Через Vercel Dashboard**

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Deployments**
4. Найдите последний деплой
5. Нажмите **"..." → Redeploy**
6. Выберите **"Use existing Build Cache"** = **ОТКЛЮЧИТЬ** (uncheck)
7. Нажмите **"Redeploy"**

**Вариант B: Через Vercel CLI**

```bash
# Установите Vercel CLI (если не установлен)
npm i -g vercel

# Локально очистите кеш
rm -rf .next
rm -rf node_modules/.cache

# Деплой с очисткой кеша
vercel --prod --force
```

**Вариант C: Через GitHub (создать пустой коммит)**

```bash
git commit --allow-empty -m "chore: Force rebuild"
git push origin master
```

---

### 2. Проверить Production Branch в Vercel

1. Откройте Vercel Dashboard
2. Выберите проект → **Settings → Git**
3. Проверьте **Production Branch**:
   - Должно быть: `master` или `main`
   - Если указана другая ветка, измените на нужную

---

### 3. Проверить активный Deployment

1. В Vercel Dashboard → **Deployments**
2. Найдите последний deployment со статусом **"Ready"**
3. Убедитесь, что это **Production** deployment (зелёный индикатор)
4. Если это Preview, нажмите **"Promote to Production"**

---

### 4. Очистить браузерный кеш

**Chrome/Edge:**
- `Ctrl + Shift + Delete` (Windows) / `Cmd + Shift + Delete` (Mac)
- Выберите "Cached images and files"
- Или `Ctrl + F5` для hard refresh

**Firefox:**
- `Ctrl + Shift + Delete`
- Выберите "Cache"
- Или `Ctrl + Shift + R` для hard refresh

**Safari:**
- `Cmd + Option + E` (очистить кеш)
- Или `Cmd + Shift + R` для hard refresh

**Или через DevTools:**
- Откройте DevTools (`F12`)
- Правой кнопкой на кнопку обновления → **"Empty Cache and Hard Reload"**

---

### 5. Добавить Cache-Control заголовки

Добавьте в `vercel.json` заголовки для инвалидации кеша:

```json
{
  "headers": [
    {
      "source": "/_next/static/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=31536000, immutable"
        }
      ]
    },
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        }
      ]
    }
  ]
}
```

---

### 6. Проверить переменные окружения

Иногда проблема в кеше переменных окружения:

1. Vercel Dashboard → **Settings → Environment Variables**
2. Проверьте, что все нужные переменные добавлены
3. Пересоберите проект после изменения переменных

---

### 7. Проверить логи деплоя

1. Vercel Dashboard → **Deployments**
2. Выберите последний deployment
3. Откройте **Build Logs**
4. Проверьте, что сборка прошла успешно
5. Проверьте, что используется правильная ветка/коммит

---

## 🔧 Быстрое решение:

Если ничего не помогает, выполните полную пересборку:

```bash
# 1. Очистите локальный кеш
rm -rf .next
rm -rf node_modules/.cache
rm -rf .vercel

# 2. Установите зависимости заново
npm ci

# 3. Создайте пустой коммит для форсирования деплоя
git commit --allow-empty -m "chore: Force Vercel rebuild"

# 4. Запушьте
git push origin master

# 5. В Vercel Dashboard:
#    - Откройте последний deployment
#    - Отключите "Use existing Build Cache"
#    - Нажмите "Redeploy"
```

---

## 📋 Чек-лист для проверки:

- [ ] Production branch настроен правильно (`master` или `main`)
- [ ] Последний deployment помечен как "Production" (не Preview)
- [ ] Build logs показывают успешную сборку
- [ ] В логах виден правильный коммит/ветка
- [ ] Пересобрано БЕЗ кеша (uncheck "Use existing Build Cache")
- [ ] Браузерный кеш очищен (hard refresh)
- [ ] Переменные окружения настроены правильно
- [ ] Vercel автоматически деплоит при push (Git Integration активна)

---

## 🆘 Если проблема остаётся:

1. **Проверьте версию коммита:**
   - В Vercel Dashboard → Deployments → выберите deployment
   - Проверьте "Commit" - должен совпадать с последним коммитом на GitHub

2. **Проверьте время деплоя:**
   - Убедитесь, что последний deployment был создан после ваших изменений

3. **Свяжитесь с поддержкой Vercel:**
   - Если ничего не помогает, обратитесь в поддержку Vercel через dashboard

