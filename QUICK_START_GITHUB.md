# 🚀 Быстрый старт: Подключение к GitHub API

## ✅ Что уже настроено:

1. ✅ Скрипты мониторинга (`.github/scripts/`)
2. ✅ Конфигурация Cursor (`.cursor/github-config.json`)
3. ✅ Документация (`docs/GITHUB_INTEGRATION.md`)

## 🔧 Настройка (1 раз):

### Windows PowerShell:

```powershell
# 1. Установите токен для текущей сессии
# ВАЖНО: Замените YOUR_TOKEN_HERE на ваш реальный токен
$env:GITHUB_TOKEN = "YOUR_TOKEN_HERE"

# 2. Проверьте подключение
.\github\scripts\monitor-deploy.ps1
```

### Для постоянного использования:

```powershell
# Установите как пользовательскую переменную (сохранится после перезагрузки)
# ВАЖНО: Замените YOUR_TOKEN_HERE на ваш реальный токен
[System.Environment]::SetEnvironmentVariable("GITHUB_TOKEN", "YOUR_TOKEN_HERE", "User")

# Обновите текущую сессию
$env:GITHUB_TOKEN = [System.Environment]::GetEnvironmentVariable("GITHUB_TOKEN", "User")
```

## 📊 Использование:

### Проверка статуса деплоя:
```powershell
.\github\scripts\monitor-deploy.ps1
```

### Через GitHub CLI (если установлен):
```powershell
gh run list --limit 1
gh run view --log
```

## 🔍 Что показывает скрипт:

- ✅ **Workflow name** - название workflow
- ✅ **Статус** - in_progress / completed
- ✅ **Результат** - success / failure
- ✅ **URL** - прямая ссылка на лог
- ✅ **Failed jobs** - список упавших jobs (если есть)

## 📝 Пример вывода:

```
📊 Статус деплоя:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Workflow: Deploy to Vercel
Статус: completed
Результат: success
Создан: 2025-12-28T09:23:07Z
URL: https://github.com/TimurSama/Civilization-Protocol/actions/runs/20551798092
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Деплой успешен!
```

## 🎯 Теперь я могу:

- ✅ Проверять статус деплоя через API
- ✅ Получать логи ошибок автоматически
- ✅ Мониторить workflow в реальном времени

Просто скажите: **"проверь статус деплоя"** или **"что с деплоем?"** и я запущу скрипт!

